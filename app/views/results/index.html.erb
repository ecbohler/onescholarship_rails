<p id="notice"><%= notice %></p>

<div class="container-fluid bg-blue jumbotron">
  <h1>My Scholarship Results</h1>
</div>

<div class="container-fluid">

  <%= form_tag apply_student_results_path, :method => :put do %>

  <% @scholarship.each do |match| %>
  <div class="row">
    <div class="col-md-8">
      <%= link_to "#{match.name}", student_scholarship_path(@student, @scholarship) %>
      <br>
      <br>
    </div>

  </div>
  <% end %>

  <div class="row">
    <div class="col-md-8">
    <%= submit_tag "Apply" %>
    <!-- <button class="btn btn-default"> -->
      <!-- Apply -->
      <% end %>
    </div>
      </div>
    <!-- </button> -->
  </div>

</div>
